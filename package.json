{
  "name": "ai-knowledge-base-rag",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm --prefix frontend run build && node -e \"const fs=require('fs'); if (!fs.existsSync('dist')) fs.mkdirSync('dist', {recursive:true}); fs.cpSync('frontend/dist', 'dist', {recursive:true});\""
  }
}
